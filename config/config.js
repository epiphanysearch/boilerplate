// Build configuration for Boilerplate
// This is only here so grunt picks it up automatically. We want the
// prefixes file to be the primary as it will sit at the top of the
// directory when sorted alphabetically.
module.exports = require('./_config');
